# Saakshi V2

Independent prototype. Nothing is submitted to the official portal.

```bash
cp .env.example .env.local
npm install
npm test
npm run dev
```

Put a Gemini API key in `.env.local` for voice transcription and category help. Demo OTP: `123456`.

Dev server: http://localhost:3001
